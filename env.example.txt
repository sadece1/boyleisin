# CampScape Docker Compose Environment Variables
# Bu dosyayı .env olarak kopyalayın: cp env.example.txt .env

# MySQL Configuration
MYSQL_ROOT_PASSWORD=CampscapeRoot2025!
DB_PASSWORD=CampscapeApp2025!
MYSQL_PORT=3306

# Backend Configuration
BACKEND_PORT=3000
JWT_SECRET=CampscapeJWTSecret2025!
JWT_EXPIRES_IN=7d
ENABLE_VIRUS_SCAN=false
REQUIRE_VIRUS_SCAN=false
ENABLE_CSRF=true
HTTPS_ENFORCE=false
ALLOWED_ORIGINS=http://frontend:80,http://localhost:8080

# Frontend Configuration
FRONTEND_PORT=8080

# File Upload
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/webp

# Frontend Build Variables (Vite)
VITE_API_BASE_URL=http://localhost:3000/api
VITE_APP_NAME=CampScape
VITE_APP_DESCRIPTION=Doğada unutulmaz kamp deneyimleri için kamp alanları ve kiralık kamp malzemeleri

